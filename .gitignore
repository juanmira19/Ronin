# Secrets — nunca se versionan, ni en repos privados
APIKEYGROQ.md
.env
*.env

# Datos de sesiones reales — ver README (Privacidad)
data/raw/

# Exports de Health Auto Export sueltos en la raiz
HealthAutoExport-*.json
HealthAutoExport-*.csv
*.gpx

__pycache__/
*.pyc

# Entorno virtual local
.venv/
# La cache de la demo (app/cache/) SI se versiona: son salidas reales del modelo
# y permiten correr la demo sin API key ni red.

# capturas que el asistente dejo en la carpeta del proyecto
Claude outputs/

# configuracion local del asistente
.claude/
